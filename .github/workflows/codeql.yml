name: CodeQL

on:
  workflow_dispatch:
  push:
    branches: [mattboxx]
  pull_request:
    branches: [mattboxx]
  schedule:
    - cron: '0 8 * * 1'

jobs:
  analyze:
    name: Analyze ${{ matrix.language }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        # c-cpp = the ESP-IDF firmware (main/ + components/), scanned buildless
        #         so no cross-compile toolchain is needed in CI.
        # python = the build/test tooling (tools/, gen scripts).
        language: [c-cpp, python]

    steps:
      - uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: none
          config-file: ./.github/codeql/codeql-config.yml

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
